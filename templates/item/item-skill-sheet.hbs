<section class="tab {{tabs.form.id}} {{tabs.form.cssClass}} scrollable" id="form"
    data-tab="{{tabs.form.id}}" data-group="{{tabs.form.group}}">
    <div class='resource'>
          <label class='resource-label'>value</label>
          <input
            type='text'
            name='system.value'
            value='{{system.value}}'
            data-dtype='Number'
          />
        </div>
</section>